server {
    listen 80;
    location /uploads/ {
        alias /var/www/rollcall/uploads/;
        autoindex off;
    }
    location / {
        proxy_pass http://backend:8080/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
